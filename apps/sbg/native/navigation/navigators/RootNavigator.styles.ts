import { StyleSheet } from "react-native";
import { tokens } from "@ppb/the-wall-common/base-theme";

export default StyleSheet.create({
  cetStackNavigatorWrapper: {
    flex: 1,
  },
  skyBetbottomBarWrapper: {
    flex: 1,
    backgroundColor: tokens.ComponentsNavigationBottombarBackgroundDefault,
  },
});
