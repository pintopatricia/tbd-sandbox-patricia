"edges": [
{{#each edges}}
    {{#if node}}
      {
        {{! TODO: cursor}}
        {{#with node}}
          "node": {
              {{> bff-obb-card}}
          }
        {{/with}}
      } {{#unless @last}},{{/unless}}
    {{else}}
      null{{#unless @last}},{{/unless}}
    {{/if}}
{{/each}}
]
{{#if pageInfo}},
  {{> bff-page-info}}
{{/if}}