"__typename": "ViewLink",
      {{#if viewUrn}}
         "viewUrn": "{{{viewUrn}}}",
      {{else}}
         "viewUrn": "mockerURN",
      {{/if}}
      {{#if viewUrl}}
         "viewUrl": "{{{viewUrl}}}",
      {{else}}
         "viewUrl": "mockerUrl",
      {{/if}}
      {{#if viewDisplayMode}}
         "viewDisplayMode": "{{{viewDisplayMode}}}"
      {{else}}
         "viewDisplayMode": null
      {{/if}}
