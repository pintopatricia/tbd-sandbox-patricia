const styles = require("./SelectableItemsCardGroup.web.modules.json");

const TEST_ID = styles.container;

module.exports = {
  TEST_ID,
  TITLE: `${TEST_ID} ${styles.title}`,
  CARD_CONTAINER: `${TEST_ID} ${styles.cardContainer}`,
};
