const styles = require("./ObbSquadBetPlayerPicker.web.modules.json");

const TEST_ID = styles.container;

module.exports = {
  TEST_ID,
  PLAYERS_LIST: `${styles.playerList}`,
  SQUAD_BET_CARD: `${styles.squadbetCardContainer}`,
};
