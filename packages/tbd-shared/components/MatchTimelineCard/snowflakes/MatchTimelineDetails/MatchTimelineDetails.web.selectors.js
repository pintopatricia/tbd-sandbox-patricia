const styles = require("./MatchTimelineDetails.web.modules.json");

const TEST_ID = styles.matchTimelineDetails;

module.exports = {
  TEST_ID,
  TIMELINE_WRAPPER: `${styles.matchTimelineWrapper}`,
  INCIDENTS_WRAPPER: `${styles.incidentEventsWrapper}`,
  MINUTE_BY_MINUTE: `${styles.minuteByMinute}`,
  TITLE_MINUTE: `${styles.minuteByMinuteTitle}`,
};
