const MINUTE_INCIDENTS = "minute-incidents";
const MINUTE_INCIDENTS_CONTAINER = `${MINUTE_INCIDENTS}-container`;
const TIME_LABEL = `${MINUTE_INCIDENTS}-time-label`;
const EXTRA_TIME_LABEL = `${MINUTE_INCIDENTS}-extra-time-label`;

module.exports = {
  MINUTE_INCIDENTS,
  MINUTE_INCIDENTS_CONTAINER,
  TIME_LABEL,
  EXTRA_TIME_LABEL,
};
