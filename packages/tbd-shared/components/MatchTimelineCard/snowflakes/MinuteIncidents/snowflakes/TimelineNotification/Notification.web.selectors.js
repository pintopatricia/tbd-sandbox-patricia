const styles = require("./Notification.web.modules.json");

const TEST_ID = styles.card;

module.exports = {
  TEST_ID,
  TITLE: `${styles.middleTitle}`,
  DESCRIPTION: `${styles.middleDescription}`,
};
