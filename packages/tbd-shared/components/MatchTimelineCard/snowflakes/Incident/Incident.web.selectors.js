const styles = require("./Incident.web.modules.json");

const TEST_ID = styles.container;
module.exports = {
  TEST_ID,
  INCIDENT_MINUTE: `${styles.minute}`,
  INCIDENT_PLAYER: `${styles.player}`,
  INCIDENT_ICON: `${styles.iconWrapper}`,
};
