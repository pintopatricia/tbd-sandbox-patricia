const styles = require("./MatchTimeline.web.modules.json");

const TEST_ID = styles.matchTimeline;

module.exports = {
  TEST_ID,
  PREMATCH: styles.prematch,
  TIMELINE_CONTAINER: styles.timelineContainer,
  TIMELINE_BARS: styles.timeline,
  TEAM_CRESTS: styles.teamCrests,
  TIMELINES: styles.timeline,
};
