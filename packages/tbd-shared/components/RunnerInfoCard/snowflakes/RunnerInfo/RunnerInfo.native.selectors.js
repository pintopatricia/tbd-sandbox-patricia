const RUNNER_INFO = "runner-info";
const RUNNER_INFO_SILK = `${RUNNER_INFO}-silk`;
const RUNNER_INFO_SILK_DEFAULT = `${RUNNER_INFO}-silk-default`;
const RUNNER_INFO_NAME = `${RUNNER_INFO}-name`;
const RUNNER_INFO_JOCKEY_LABEL = `${RUNNER_INFO}-jockey-label`;
const RUNNER_INFO_JOCKEY_NAME = `${RUNNER_INFO}-jockey-name`;
const RUNNER_INFO_TRAINER_LABEL = `${RUNNER_INFO}-trainer-label`;
const RUNNER_INFO_TRAINER_NAME = `${RUNNER_INFO}-trainer-name`;

module.exports = {
  RUNNER_INFO,
  RUNNER_INFO_SILK,
  RUNNER_INFO_SILK_DEFAULT,
  RUNNER_INFO_NAME,
  RUNNER_INFO_JOCKEY_LABEL,
  RUNNER_INFO_JOCKEY_NAME,
  RUNNER_INFO_TRAINER_LABEL,
  RUNNER_INFO_TRAINER_NAME,
};
