const EXPANDABLE_CARDGROUP = "expandable-cardgroup";
const EXPANDABLE_CARDGROUP_HEADER_CONTAINER = `${EXPANDABLE_CARDGROUP}-header-container`;
const EXPANDABLE_CARDGROUP_ITEM = `${EXPANDABLE_CARDGROUP}-item`;

module.exports = {
  EXPANDABLE_CARDGROUP,
  EXPANDABLE_CARDGROUP_HEADER_CONTAINER,
  EXPANDABLE_CARDGROUP_ITEM,
};
