const styles = require("./ExpandableCardGroup.web.modules.json");

const TEST_ID = styles.expandableCardgroup;

module.exports = {
  TEST_ID,
  EXPANDABLE_CARDGROUP_HEADER_CONTAINER: styles.headerContainer,
  EXPANDABLE_CARDGROUP_HEADER_TITLE: styles.headerTitle,
  EXPANDABLE_CARDGROUP_ITEM: styles.expandableCardgroupItem,
};
