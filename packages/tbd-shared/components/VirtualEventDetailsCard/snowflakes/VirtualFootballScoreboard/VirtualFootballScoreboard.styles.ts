import { StyleSheet } from "react-native";
import { tokens } from "@ppb/the-wall-common/base-theme";

export const separator = StyleSheet.create({
  virtualShape: {
    backgroundColor: tokens.TeamTextLabelColour,
  },
});
