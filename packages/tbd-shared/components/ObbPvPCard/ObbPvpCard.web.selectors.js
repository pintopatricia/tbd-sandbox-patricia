const styles = require("./ObbPvPCard.web.modules.json");

const TEST_ID = styles.cardContainer;

module.exports = {
  TEST_ID,
  PLAYER_PARTICIPANTS_CONTAINER: `${styles.playerParticipantsContainer}`,
  PLAYER_PARTICIPANT_CONTAINER: `${styles.playerParticipantContainer}`,
};
