const styles = require("./SuccessfulDepositContent.web.modules.json");

const TEST_ID = styles.successfulDeposit;

module.exports = {
  TEST_ID,
  ICON: `${styles.successfulDepositIcon}`,
  TITLE: `${TEST_ID} > h2`,
  SUBTITLE: `${TEST_ID} > p`,
};
