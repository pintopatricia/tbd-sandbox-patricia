const styles = require("./CashBalancesSimpleView.web.modules.json");

const TEST_ID = styles.cashBalanceSimpleViewContainer;

module.exports = {
  TEST_ID,
  BALANCE_TITLE: `${styles.title}`,
  BALANCE_AMOUNT: `${styles.amount}`,
  BALANCE_HIDDEN: `${styles.hiddenContent}`,
};
