const styles = require("./RewardCard.web.modules.json");

const TEST_ID = styles.box;

module.exports = {
  TEST_ID,
  BOX: TEST_ID,
  ICON_WRAPPER: `${styles.iconWrapper}`,
  TITLE_WRAPPER: `${styles.titleWrapper}`,
  TITLE: `${styles.title}`,
};
