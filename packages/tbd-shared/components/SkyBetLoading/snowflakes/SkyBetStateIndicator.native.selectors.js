const STATE_INDICATOR = "state-indicator";
const STATE_INDICATOR_VERSION = `${STATE_INDICATOR}-version`;
const STATE_INDICATOR_TITLE = `${STATE_INDICATOR}-title`;
const STATE_INDICATOR_EFL_IMAGE = `${STATE_INDICATOR}-efl-image`;
const STATE_INDICATOR_EFL_TEXT = `${STATE_INDICATOR}-efl-text`;
const STATE_INDICATOR_BRITISH_HEART_FOUNDATION_IMAGE = `${STATE_INDICATOR}-british-heart-foundation-image`;
const STATE_INDICATOR_SAFER_GAMBLING_IMAGE = `${STATE_INDICATOR}-safer-gambling-image`;
module.exports = {
  STATE_INDICATOR,
  STATE_INDICATOR_VERSION,
  STATE_INDICATOR_TITLE,
  STATE_INDICATOR_EFL_IMAGE,
  STATE_INDICATOR_EFL_TEXT,
  STATE_INDICATOR_SAFER_GAMBLING_IMAGE,
  STATE_INDICATOR_BRITISH_HEART_FOUNDATION_IMAGE,
};
