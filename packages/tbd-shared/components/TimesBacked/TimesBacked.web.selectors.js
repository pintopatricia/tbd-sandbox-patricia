const styles = require("./TimesBacked.web.modules.json");

const TEST_ID = styles.timesBackedContainer;

module.exports = {
  TEST_ID,
  TIMES_BACKED_LABEL: `${TEST_ID} ${styles.timesBackedLabel}`,
};
