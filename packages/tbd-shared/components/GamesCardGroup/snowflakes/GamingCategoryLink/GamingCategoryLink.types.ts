import type { CardIconTypes, ViewLink } from "@ppb/the-wall-common/types";

export type GamingCategoryLinkProps = {
  viewLink: ViewLink;
  cardIcon?: CardIconTypes;
  label: string;
  buttonText: string;
};
