const TEST_ID = "enhanced-tracking";
const TRACKING_BAR = `${TEST_ID}-tracking-bar`;
const INDIVIDUAL_TRACKING = `${TEST_ID}-individual-tracking`;
const PLAYER_NAME = `${INDIVIDUAL_TRACKING}-player-name`;

module.exports = {
  TEST_ID,
  TRACKING_BAR,
  INDIVIDUAL_TRACKING,
  PLAYER_NAME,
};
