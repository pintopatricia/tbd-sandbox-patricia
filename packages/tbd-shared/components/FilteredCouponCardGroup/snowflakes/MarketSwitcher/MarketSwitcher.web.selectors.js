const styles = require("./MarketSwitcher.web.modules.json");

const TEST_ID = styles.marketSwitcherContainer;

module.exports = {
  TEST_ID,
  BUTTON: styles.marketSwitcher,
  TITLE: styles.title,
  LABEL: styles.label,
  CHEVRON: `${styles.icon}`,
};
