const HEADER = "header";
const HEADER_BACK_BUTTON = `${HEADER}-back-button`;
const HEADER_LOGO = `${HEADER}-logo`;
const HEADER_BALANCE_BUTTON = `${HEADER}-balance-button`;
const HEADER_BALANCE_PRESSABLE = `${HEADER}-balance-pressable`;
const HEADER_BALANCE_LABEL = `${HEADER}-balance-label`;
const HEADER_GENEROSITY_WALLET_PRESSABLE = `${HEADER}-generosity-wallet-pressable`;
const HEADER_USER_PROFILE = `${HEADER}-user-profile`;
const LOGIN_CONTAINER = "login-container";
const JOIN_NOW_BUTTON = `${LOGIN_CONTAINER}-join-now-button`;
const LOGIN_BUTTON = `${LOGIN_CONTAINER}-login-button`;
const HEADER_MENU_BUTTON = `${HEADER}-menu-button`;
const HEADER_GAMES_LOGO = `${HEADER}-games-logo`;

module.exports = {
  HEADER,
  HEADER_BACK_BUTTON,
  HEADER_LOGO,
  HEADER_BALANCE_BUTTON,
  HEADER_BALANCE_PRESSABLE,
  HEADER_BALANCE_LABEL,
  HEADER_GENEROSITY_WALLET_PRESSABLE,
  HEADER_USER_PROFILE,
  JOIN_NOW_BUTTON,
  LOGIN_BUTTON,
  LOGIN_CONTAINER,
  HEADER_MENU_BUTTON,
  HEADER_GAMES_LOGO,
};
