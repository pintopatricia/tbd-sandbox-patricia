import { StyleSheet } from "react-native";
import { colors } from "@ppb/the-wall-common/base-theme";

export default StyleSheet.create({
  marketRulesContainer: {
    height: "100%",
  },
  content: {
    width: "100%",
    backgroundColor: colors.NeutralsBackgroundElevation1,
  },
});
