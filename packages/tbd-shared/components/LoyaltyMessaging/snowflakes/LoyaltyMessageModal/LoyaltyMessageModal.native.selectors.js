const TEST_ID = "loyalty-message-modal";
const CONTAINER = `${TEST_ID}-container`;
const MESSAGE_CONTAINER = `${TEST_ID}-message-container`;
const MESSAGE = `${TEST_ID}-message`;
const TC_BUTTON = `${TEST_ID}-terms-button`;

module.exports = {
  TEST_ID,
  CONTAINER,
  MESSAGE_CONTAINER,
  MESSAGE,
  TC_BUTTON,
};
