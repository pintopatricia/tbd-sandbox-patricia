const TEST_ID = "modal-native";
const OVERLAY = `${TEST_ID}-overlay`;
const ACTION_LINK = `${TEST_ID}-action-link`;
const PRIMARY_BUTTON = `${TEST_ID}-primary-button`;
const MODAL = `${TEST_ID}-modal`;

module.exports = {
  TEST_ID,
  OVERLAY,
  ACTION_LINK,
  PRIMARY_BUTTON,
  MODAL,
};
