import { StyleSheet } from "react-native";

export default StyleSheet.create({
  placeholder: {
    width: "100%",
    height: 600,
  },
  temporaryPlaceholderOverride: {
    height: "100%",
  },
});
