const styles = require("./InlineMarketRunner.web.modules.json");

const TEST_ID = styles.container;

module.exports = {
  TEST_ID,
  INLINE_MARKET_RUNNER_HANDICAP: `${TEST_ID} ${styles.handicap}`,
};
