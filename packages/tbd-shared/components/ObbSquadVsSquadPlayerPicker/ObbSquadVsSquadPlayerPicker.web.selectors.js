const styles = require("./ObbSquadVsSquadPlayerPicker.web.modules.json");

const TEST_ID = styles.container;

module.exports = {
  TEST_ID,
  PLAYERS_LIST: `${styles.playerList}`,
  SQUAD_VS_SQUAD_CARD: `${styles.squadVsSquadCardContainer}`,
};
