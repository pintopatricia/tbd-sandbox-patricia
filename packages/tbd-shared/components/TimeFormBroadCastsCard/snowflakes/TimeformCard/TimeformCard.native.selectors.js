const TIMEFORM_CARD = "timeform-card";
const TIMEFORM_CARD_CONTENT = `${TIMEFORM_CARD}-content`;
const TIMEFORM_CARD_RUNNER_RATING = `${TIMEFORM_CARD}-runner-rating`;
const TIMEFORM_CARD_RUNNER_RATING_TEXT = `${TIMEFORM_CARD}-runner-rating-text`;
const TIMEFORM_CARD_VERDICT_SECTION = `${TIMEFORM_CARD}-verdict-section`;
const TIMEFORM_CARD_VERDICT_LABEL = `${TIMEFORM_CARD}-verdict-label`;
const TIMEFORM_CARD_VERDICT_TEXT = `${TIMEFORM_CARD}-verdict`;

module.exports = {
  TIMEFORM_CARD,
  TIMEFORM_CARD_CONTENT,
  TIMEFORM_CARD_RUNNER_RATING,
  TIMEFORM_CARD_RUNNER_RATING_TEXT,
  TIMEFORM_CARD_VERDICT_SECTION,
  TIMEFORM_CARD_VERDICT_LABEL,
  TIMEFORM_CARD_VERDICT_TEXT,
};
