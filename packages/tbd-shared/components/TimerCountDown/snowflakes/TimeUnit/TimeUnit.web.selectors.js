const styles = require("./TimeUnit.web.modules.json");

const TEST_ID = styles.timeUnit;

module.exports = {
  TEST_ID,
  TIME_UNIT_VALUES: `${styles.timerBorder}`,
  TIME_UNIT_TEXT: `${styles.text}`,
};
