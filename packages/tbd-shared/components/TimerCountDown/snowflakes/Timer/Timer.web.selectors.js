const styles = require("./Timer.web.modules.json");

const TEST_ID = styles.timerContainer;

module.exports = {
  TEST_ID,
  TIMER_LABEL: `${styles.timerTopLabel}`,
  TIMER_VALUES: `${styles.countDownTimer}`,
};
