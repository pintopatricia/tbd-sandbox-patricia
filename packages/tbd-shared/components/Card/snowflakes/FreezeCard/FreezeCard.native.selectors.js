const FREEZE_CARD = "freeze-card";
const FREEZE_DROP_SHADOW = `${FREEZE_CARD}-drop-shadow`;
const FREEZE_CARD_TOP = `${FREEZE_CARD}-top`;
const FREEZE_CARD_BOTTOM = `${FREEZE_CARD}-bottom`;
const FREEZE_CARD_DIVIDER = `${FREEZE_CARD}-divider`;
const FREEZE_CARD_CONTENT = `${FREEZE_CARD}-content`;
const FREEZE_CARD_DYNAMIC = `${FREEZE_CARD}-dynamic`;
const FREEZE_CARD_ICON = `${FREEZE_CARD}-icon`;
const FREEZE_CARD_LABEL = `${FREEZE_CARD}-label`;
const FREEZE_CARD_CONTENT_TEXT = `${FREEZE_CARD}-content-text`;
const FREEZE_CARD_STATUS_LABEL = `${FREEZE_CARD}-status-label`;

module.exports = {
  FREEZE_CARD,
  FREEZE_DROP_SHADOW,
  FREEZE_CARD_TOP,
  FREEZE_CARD_BOTTOM,
  FREEZE_CARD_DIVIDER,
  FREEZE_CARD_CONTENT,
  FREEZE_CARD_DYNAMIC,
  FREEZE_CARD_ICON,
  FREEZE_CARD_LABEL,
  FREEZE_CARD_CONTENT_TEXT,
  FREEZE_CARD_STATUS_LABEL,
};
