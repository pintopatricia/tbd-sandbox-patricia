const styles = require("./FreezeCard.web.modules.json");

const TEST_ID = styles.freezeCard;

module.exports = {
  TEST_ID,
  TOP: styles.top,
  BOTTOM: styles.bottom,
  DIVIDER: styles.divider,
  CONTENT: styles.content,
  DYNAMIC: styles.dynamic,
  ICON: styles.icon,
  LABEL: styles.label,
  CONTENT_TEXT: styles.contentText,
  STATUSLABEL: styles.statusLabel,
};
