const styles = require("./PromoMessageCard.web.modules.json");

const TEST_ID = styles.imsPromoMessage;

module.exports = {
  TEST_ID,
  INFO_TEXT: `${styles.infoText}`,
  RECOMMENDED_TEXT: `${styles.recommendedText}`,
  BUTTON: `${TEST_ID} button`,
};
