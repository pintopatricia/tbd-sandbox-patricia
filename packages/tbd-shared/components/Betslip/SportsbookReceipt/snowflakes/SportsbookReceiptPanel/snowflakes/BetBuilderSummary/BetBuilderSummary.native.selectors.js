const BET_BUILDER_SUMMARY = "bet-builder-summary";
const TITLE = `${BET_BUILDER_SUMMARY}-lines`;
const SELECTIONS = `${BET_BUILDER_SUMMARY}-selections]`;
const SUMMARY = `${BET_BUILDER_SUMMARY}-summary]`;

module.exports = {
  BET_BUILDER_SUMMARY,
  TITLE,
  SELECTIONS,
  SUMMARY,
};
