const styles = require("./BetBuilderSummary.web.modules.json");

const TEST_ID = styles.bet;

module.exports = {
  TEST_ID,
  TITLE: `${styles.title}`,
  SELECTIONS: `${styles.selections}`,
  SUMMARY: `${styles.summary}`,
};
