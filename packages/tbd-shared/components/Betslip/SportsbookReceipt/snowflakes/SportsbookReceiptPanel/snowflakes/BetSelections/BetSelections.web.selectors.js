const styles = require("./BetSelections.web.modules.json");

const TEST_ID = styles.container;
const HEADER = styles.header;
const HEADER_TITLE = styles.title;
const CONTENT = styles.selections;

module.exports = {
  TEST_ID,
  HEADER_TITLE,
  HEADER,
  CONTENT,
};
