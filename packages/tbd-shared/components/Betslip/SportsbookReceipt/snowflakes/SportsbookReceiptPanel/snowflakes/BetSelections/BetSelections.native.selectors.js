const BET_SELECTIONS = "sportsbook-multiples-composition";
const CONTENT = `${BET_SELECTIONS}-content`;
const HEADER = `${BET_SELECTIONS}-collapse-header`;
const SELECTION = `${BET_SELECTIONS}-selection`;

module.exports = {
  BET_SELECTIONS,
  CONTENT,
  HEADER,
  SELECTION,
};
