const styles = require("./PlacedBetCard.web.modules.json");

const TEST_ID = styles.card;

module.exports = {
  TEST_ID,
  HEADER: `${styles.header}`,
  RESULTS: `${styles.results}`,
  FREE_BETS: `${styles.freeBets}`,
};
