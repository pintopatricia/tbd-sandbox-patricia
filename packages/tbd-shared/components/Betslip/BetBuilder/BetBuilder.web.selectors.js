const styles = require("./BetBuilder.web.modules.json");

const TEST_ID = styles.bet;

module.exports = {
  TEST_ID,
  TITLE: `${styles.title}`,
  SUBTITLE: `${styles.subtitle}`,
  POPULAR_BADGE: `${styles.popularBadge}`,
  POPULAR_ICON: `${styles.popularIcon}`,
  POPULAR_LABEL: `${styles.popularLabel}`,
};
