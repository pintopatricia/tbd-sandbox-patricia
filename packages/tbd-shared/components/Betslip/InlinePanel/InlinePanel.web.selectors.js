const styles = require("./InlinePanel.web.modules.json");

module.exports = {
  TEST_ID: styles.inlinePanel,
  TITLE_PREFIX: `${styles.titlePrefix}`,
  TITLE: `${styles.title}`,
  ACTION: `${styles.action}`,
  CONTENT: `${styles.content}`,
};
