const INLINE_PANEL = "inline-panel";
const TITLE_PREFIX = `${INLINE_PANEL}-titlePrefix`;
const TITLE = `${INLINE_PANEL}-title`;
const ACTION = `${INLINE_PANEL}-action`;
const CONTENT = `${INLINE_PANEL}-content`;

module.exports = {
  INLINE_PANEL,
  TITLE_PREFIX,
  TITLE,
  ACTION,
  CONTENT,
};
