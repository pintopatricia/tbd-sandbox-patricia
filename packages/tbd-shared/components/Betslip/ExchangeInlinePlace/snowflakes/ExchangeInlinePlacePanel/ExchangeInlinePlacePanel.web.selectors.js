const styles = require("./ExchangeInlinePlacePanel.web.modules.json");

const TEST_ID = styles.container;

module.exports = {
  TEST_ID,
  INPUTS: styles.input,
  FREE_BETS: styles.freebets,
  NOTIFICATIONS: styles.notifications,
  PLACE_BUTTON: styles.placeButton,
  QUICKSTAKES: styles.quickStakes,
};
