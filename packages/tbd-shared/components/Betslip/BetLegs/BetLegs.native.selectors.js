const BET_LEGS = "bet-legs";
const SELECTION = `${BET_LEGS}-selection`;
const DESCRIPTION = `${BET_LEGS}-description`;
const BET_LEGS_CONTENT = `${BET_LEGS}-content`;

module.exports = {
  BET_LEGS,
  SELECTION,
  DESCRIPTION,
  BET_LEGS_CONTENT,
};
