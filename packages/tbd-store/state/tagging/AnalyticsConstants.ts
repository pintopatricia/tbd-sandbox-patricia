export enum BetDirection {
  Back = "back",
  Lay = "lay",
}

export enum BetTypeGroup {
  Single = "single",
  Multiple = "multiple",
}

export enum YesNo {
  Yes = "yes",
  No = "no",
}

export enum CashoutType {
  Full = "full",
  Partial = "partial",
}

export enum PlatformType {
  Web = "web",
  Native = "native",
  Wrapper = "wrapper",
}
